# 验证记录

日期：2026-09-21。此记录区分真实运行、假设描述下的人工文本检查和宿主集成验证，不把示例审阅等同于 WorkBuddy 实测。

## 1.2.0 照片显示调整验证

- 新增经过校验的 `photo` 参数，支持不透明度、contain/cover、对齐、图框比例、底色及圆角；原图保持原字节，不重新编码。
- 实际运行标准库测试：12 项全部通过。新增照片显示与原字节一致、cover 需显式图框比例、非法数值/枚举/CSS 注入拒绝、无原图时拒绝照片调整；旧输入与自定义模板测试仍通过。
- 对 CC0 竖幅示例和用户横幅照片的新成品分别在 Chrome 1440、390、320px 实际检查：图片加载成功，opacity 为 0.85，object-fit 为 contain，位置为 50% 50%，圆角 16px，框比例分别为 3:4 / 4:3，文字 opacity 为 1；未发现页面或正文溢出，外部 HTTP 请求为 0。
- 另用 CC0 图运行 cover：16:9 图框、50% 30% 对齐均通过浏览器计算样式检查。此项不等于证明任意照片裁切后主体完整，仍需逐图预览。
- 人工查看用户调整成品的桌面及 390px 截图，确认照片变淡、文字清楚、无新增人物裁切；私人照片和成品仍未放入公开目录。公开示例见 `demo/photo-adjusted-postcard.html`，公开截图用的是 CC0 照片。
- 本次新增显示设置未单独重做打印预览；以下旧版打印记录不代表所有 photo 参数和自定义模板的打印结果。WorkBuddy 集成仍未实测。

## 1.1.0 自由排版更新验证

- 新增 `--template`，实际用不同 DOM 结构生成公开的 `demo/editorial-postcard.html`；基础版成品仍保留，便于比较。Skill 指令允许按照片重新编写 HTML/CSS，不限定模板数量；脚本本身不自动理解构图。
- 实际运行标准库测试：8 项全部通过，包含原有 6 项，以及自定义布局下文本仍被转义、缺少必要内容变量时拒绝输出的两项。
- Chrome 实际检查新的公开示例和另一张用户提供横幅照片的本地成品：各在 1440、390、320px 下加载，均无图片失败、横向或正文区域溢出，外部 HTTP 请求为 0。用户照片与对应成品未进入公开仓库。
- 新版用户成品的桌面、390px 截图已人工检查；A4 打印 PDF 经 Poppler 渲染并人工查看，当前文字一页完整呈现。对应用户图片和预览仅在本地保存；公开仓库截图 `demo/preview-editorial.png` 使用 CC0 演示照片。
- 本次变更验证不等于所有未来自定义设计都通过。每次新设计仍须检查响应式、转义、图片和打印；WorkBuddy 集成状态仍是未实测。

以下保留 1.0.0 基础模板的首次验证记录。

## A. 真实照片与工具运行

- 下载并用图像查看工具实际读取 `demo/sample.jpg`：963 × 1280，JPEG。确认落日、橙红天空、海面、浪花与前景岩石；未使用假设场景中的脚印。演示心情为明确的课堂设定。
- Pillow 解码成功，EXIF 字典为空；SHA-1 与来源文件页一致：`42bbcd2fd38655635e5becce4e152848b2db692d`。没有修改原照片字节。
- Python 3.8.10 实际执行 `scripts/render_postcard.py`，生成 `demo/example-postcard.html`，图片字节以 data URI 嵌入，中文内容读取自 `example-content.json`。
- 实际执行 `python -m unittest discover -s tests -v`：6 项通过。覆盖原图字节一致、无外部资源、文本与属性转义、恶意标签只显示为文字、相对路径副本、无图说明、拒绝覆盖、拒绝错误字段与 SVG。测试依赖仅标准库。
- Chrome 153.0.8010.48 + Playwright 实际打开成品；三种视口均检查图片解码、页面与内容区域溢出、布局列数和网络请求。

| 视口宽度 | 布局 | 图片 | 横向/正文溢出 | 外部 HTTP 请求 |
| --- | --- | --- | --- | --- |
| 1440px | 两列 556px + 556px | 加载成功 | 无 | 0 |
| 390px | 单列 358px | 加载成功 | 无 | 0 |
| 320px | 单列 288px | 加载成功 | 无 | 0 |

人工查看桌面、390px 截图：中文正常，原图完整，正文与寄语可见。公开的桌面截图为 `demo/preview-desktop.png`，不是设计稿。

Chrome 实际导出 A4 PDF，用 Poppler 渲染并人工检查一页打印结果；标题、完整故事和寄语均可见，提取文本复核无遗漏。本结果仅针对当前示例长度，不承诺任意长文单页、双面套印、裁切线或专业印刷规格。浏览器/字体更换后应再预览。

## B. 基于假设画面描述的文本行为检查

方法：人工编写并审阅 `references/examples.md` 的三个完整示例、一个改写示例及边界处理片段，对照 Skill 的输入来源和事实约束。不是独立模型自动评测，也没有把描述当作真实上传图片。

| 场景 | 已检查的文本/规则与结果 |
| --- | --- |
| 1. 海边落日 | 示例 1 使用橙天、反光、脚印；没有城市、同行者或共同经历。符合。 |
| 2. 雨后街巷 | 示例 2 使用湿石板、屋檐和灯光；轻松来自给定心情，没有失恋或痛苦背景。符合。 |
| 3. 山间云海 | 示例 3 使用山脊、云层、栏杆；语气家常，无父母往事或安全保证。符合。 |
| 4. 只给照片无心情 | 示例 6 及 Skill 默认规则：已有明信片任务时直接平实记录，默认未来的自己，不连续追问；新对话无创作意图不误触发。规则/片段审阅符合，未做宿主端上传实测。 |
| 5. 读图失败 | 示例 5 明说没能读取，索取一句描述；没有虚构视觉内容。符合。 |
| 6. 同图换收件人 | 示例 4 保留场景与舍不得，将重点改为向妈妈分享并邀请回应，不只替换称呼。符合。 |
| 7. 信息少的夜景 | 示例 7 仅用已确认亮灯，不猜招牌、城市或情绪；允许缩短与减少细节。规则/片段审阅符合，未做真实夜景上传实测。 |
| 8. HTML 成品 | 真实运行见 A；与假设文本检查分开统计。 |

## C. 包结构与发布检查

- frontmatter 通过实际 YAML 解析，核对 name、description、description_zh、description_en、version、author 等字段；`name` 为 `travel-story-postcard`。
- 配套 Skill 创建工具的 quick_validate 已运行；其 Codex 字段白名单不接受 WorkBuddy 官方字段 `author / description_en / description_zh / display_name / version`，因此报告平台字段不匹配。没有把该检查写成通过，也没有为通过 Codex 校验删掉目标平台所需字段；改用 YAML 解析与 WorkBuddy 官方字段检查。
- Markdown 本地相对链接逐项解析；模板变量与脚本字段一致；实际成品无未替换变量。包中无 `agents/openai.yaml`、额外账户配置或硬编码安装路径。
- 待发布内容仅限本目录的 Skill、说明、模板、脚本、测试与公开演示素材。扫描常见密钥格式、私钥头、Windows 用户路径、邮箱模式及本地配置文件；结合人工审阅确认未包含用户私人照片、个人身份资料或密钥。公开摄影者署名属于必要来源信息。
- Git 元数据、缓存、临时浏览器文件不纳入分发 ZIP。公开文件清单和 ZIP 内容均在打包时核对。

## D. 尚未完成的目标环境验证

- 未在用户的 WorkBuddy 中实际导入、启用或调用；不能称“通过 WorkBuddy 安装验证”。
- 没有核实某个目标版本直接通过 GitHub URL 安装；README 采用有官方入口依据的本地导入流程，ZIP 层级和资源加载仍需在客户端确认。
- 未实测 WorkBuddy 对 `assets/`、Python 调用、附件原图路径、后续多轮原图访问的行为。
- 未验证更新/重导入方式，不声称远程仓库更新会自动同步。
- 不同模型可能生成不同文本；这里的人工示例审阅不代表所有输入均已覆盖。

官方依据：[WorkBuddy 开放平台技能结构](https://open.workbuddy.cn/docs/skill)、[WorkBuddy 技能安装](https://www.workbuddy.cn/docs/workbuddy/From-Beginner-to-Expert-Guide/Function-Description/Skills-Market)。
